import './Envelope.styles.css'

import Terminal from './Terminal';

const Envelope = () => {
    return (
          <div id="envelope" >
            <Terminal />
          </div>
      )
}

export default Envelope;